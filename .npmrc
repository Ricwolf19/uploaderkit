# Public package: everything resolves from npmjs. No company registry here on
# purpose — this repo must build for anyone who clones it.

auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
